version: '3.8'

services:
  aess-chat-bus:
    build: .
    container_name: aess-m2m-chat-bus
    restart: always
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
